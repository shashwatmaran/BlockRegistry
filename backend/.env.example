# Environment Variables Configuration
# ⚠️  SECURITY WARNING: NEVER commit this file with real credentials!
# ⚠️  This file should be copied to .env and filled with actual values
# ⚠️  The .env file is gitignored and should NEVER be pushed to git
#
# Copy this file to .env and update with your actual values

# Database Configuration
MONGO_URL=mongodb+srv://your-username:your-password@cluster.mongodb.net/?retryWrites=true&w=majority
DB_NAME=land_registry

# Security Configuration
# Generate a strong random key using one of these commands:
# Linux/Mac: openssl rand -hex 32
# Windows PowerShell: -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 64 | ForEach-Object {[char]$_})
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Server Configuration (Optional)
HOST=0.0.0.0
PORT=8000
DEBUG=False
